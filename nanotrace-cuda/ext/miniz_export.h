#pragma once

// Stub export header for miniz when used as a static library
// MINIZ_EXPORT is used for DLL export/import on Windows
// For static library usage, we just make it empty
#define MINIZ_EXPORT
